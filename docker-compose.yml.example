version: '3.1'

services:
  mongo:
    image: mongo
    restart: always
    ports:
    - 27017:27017
    volumes:
    - <localpath>:/data/db
    environment:
      MONGO_INITDB_ROOT_USERNAME: yourusername
      MONGO_INITDB_ROOT_PASSWORD: yourpassword
